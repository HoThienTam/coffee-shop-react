import React from "react";
import { Container } from "react-bootstrap";
import Header from "./Header";

const Home = (props) => {
  return (
    <Container>
      <Header />
    </Container>
  );
};

export default Home;
